# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-yfs-bsp"
BBFILE_PATTERN_meta-yfs-bsp = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-yfs-bsp = "6"

LAYERDEPENDS_meta-yfs-bsp = "core"
LAYERSERIES_COMPAT_meta-yfs-bsp = "scarthgap"
