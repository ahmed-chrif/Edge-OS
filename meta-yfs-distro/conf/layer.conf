# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-yfs-distro"
BBFILE_PATTERN_meta-yfs-distro = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-yfs-distro = "6"

LAYERDEPENDS_meta-yfs-distro = "core"
LAYERSERIES_COMPAT_meta-yfs-distro = "scarthgap"
